# 🏠 Local Development Environment
# Copy nội dung này vào file .env để chạy local

# 💾 Local PostgreSQL Database  
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=111020
DB_NAME=movie

# 🔒 JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-for-bluesocial-min-32-chars
JWT_EXPIRES_IN=7d

# 🌍 Application Environment
NODE_ENV=development
PORT=3000

# 📝 Không cần DATABASE_URL cho local development
# DATABASE_URL sẽ được tự động tạo từ các biến DB_* ở trên 